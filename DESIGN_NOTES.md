# badminton-camp 改進建議（給負責人參考，不代表已採納或已修改任何檔案）

比較基準：football-camp/index.html（負責人認定的品質標竿）。
以下按優先級排列，皆為務實小改動，非重構提案。

## P0 — 會被訪客當成「還沒做完」的東西，建議優先處理

1. **頂部橫幅占位框**（`.top-banner` / `.banner-placeholder`，文字「橫幅照片準備中」）
   是訪客進站第一眼看到的東西，直接寫「準備中」等於自曝其短。football-camp 完全沒有這個橫幅，直接進 hero 也很完整。
   建議：整塊拿掉（hero 本身已經夠完整），而不是留著等照片。

2. **活動花絮區塊**（`#gallery`，4 個虛線占位框「照片準備中」+ 說明文「實際活動照片即將更新」）
   同樣是明顯的未完成訊號。football-camp 沒有這種「等內容」的區塊——它只放已經有的東西（場地實拍照）。
   建議：沒照片前，整節先拿掉，或改成像主頁改造那樣用漸層/幾何/文字重新設計成一個「賣點強化區」（例如把 1:10 分組、責任保險等賣點做成視覺區塊），避免版面出現「還沒好」的坑。

## P1 — 跟 football-camp 有明顯體驗落差，值得補

3. **FAQ 沒有手風琴互動**：目前 badminton-camp 的 FAQ 是純展開的靜態區塊，football-camp 是點擊展開/收合（`.faq-q` + `.faq-arrow` 旋轉 + `max-height` 過渡）。football 版本已經寫好且穩定，直接搬過來、換色即可，版面會更緊湊、更像同一等級的產品。

4. **手機版沒有底部懸浮報名按鈕**：football-camp 有 `.mobile-cta` 固定在螢幕底部的「立即報名」按鈕，手機瀏覽時大拇指隨時點得到；badminton-camp 手機版只靠頂部 nav 的小按鈕，捲動後不容易注意到。這段 CSS/HTML 在 football-camp 已經現成，複製並換配色 token 即可，是低成本、對轉換率有感的改動。

## P2 — 內容缺口，需要真實素材才能做，先放著沒關係

5. **完全沒有教練介紹區塊**：football-camp 有 `#coaches`，badminton-camp 沒有對應的 section。在沒有真人資料前不必編造姓名/照片，但可以先放一個像主頁「關於我們」那種只寫通則的極簡版本（專業教練依程度分階、責任保險等），比完全沒有這塊來得有安心感；等有真實教練資料再升級成 football-camp 的 `cgrid` 卡片式版面。

6. **沒有場地介紹**：football-camp 有實拍場地照（Cloudinary 圖＋地圖連結），badminton-camp 完全沒有這節。台大體育館是明確、可描述的地點，可以先做一個「純文字＋Google 地圖連結卡」的輕量版本（不需要照片），之後有實拍再升級成跟 football-camp 一樣的圖片版型。

---
以上皆為建議，優先序是「對第一印象殺傷力」與「改動成本」的權衡，不代表一定要照單全收。
